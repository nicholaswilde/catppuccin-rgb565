# CYD Weather Station — Local Repository Rule

## Rule
When looking up `cyd-weather-station` code, examples, or documentation, always read
the local repository directly at:

    /home/nicholas/git/nicholaswilde/cyd-weather-station

Do NOT perform a web search for cyd-weather-station content that is likely to exist
locally. Use `ctx_execute_file`, `ctx_search`, or `rtk read` on that path first.
